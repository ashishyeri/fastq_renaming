// Test profile: small input for quick validation.
// Run: nextflow run main.nf -profile test,conda -c config/paths.config

params {
    input_fastq_glob = "${projectDir}/tests/testdata/*.fastq.gz"
    sample_naming_convention = "illumina"
}
